---
// ServicesGrid component for russabuss.com homepage
// 6 service cards: Mix & Master, Custom Tracks, Beats, Studio Time, Vocals, DJ
import { ServiceCard } from '../react/ServiceCard';

interface Service {
  title: string;
  description: string;
  icon: string;
  href: string;
}

const services: Service[] = [
  {
    title: 'Mix & Master',
    description: 'Professional mixing and mastering to make your tracks radio-ready with clarity, punch, and polish.',
    icon: '🎛️',
    href: '/services#mixing',
  },
  {
    title: 'Custom Production',
    description: 'Original instrumentals crafted to your vision. From concept to completion, tailored to your sound.',
    icon: '🎹',
    href: '/services#production',
  },
  {
    title: 'Beat Licensing',
    description: 'Browse the catalog and lease beats instantly. MP3, WAV, trackouts, or exclusive rights available.',
    icon: '🎵',
    href: '/beats',
  },
  {
    title: 'Studio Sessions',
    description: 'Book time in a professional environment with engineering support. Recording, mixing, or production.',
    icon: '🎙️',
    href: '/services#studio',
  },
  {
    title: 'Vocal Production',
    description: 'Recording, tuning, editing, and processing to get your vocals sitting perfectly in the mix.',
    icon: '🎤',
    href: '/services#vocals',
  },
  {
    title: 'DJ Services',
    description: 'Live sets, events, and curated playlists. From intimate gatherings to large venues.',
    icon: '💿',
    href: '/services#dj',
  },
];
---

<section class="services" aria-labelledby="services-heading">
  <div class="services-container">
    <div class="services-header">
      <h2 id="services-heading" class="services-title">What I Do</h2>
      <p class="services-intro">
        Each project I take on has different needs, and I want to make sure you're 100% happy with the final copy. Here's how I can help.
      </p>
    </div>

    <div class="services-grid">
      {services.map((service) => (
        <ServiceCard client:visible {...service} />
      ))}
    </div>
  </div>
</section>

<style>
  .services {
    background: var(--color-bg-primary, #0a0a0a);
    padding: var(--space-20, 5rem) var(--space-4, 1rem);
  }

  .services-container {
    max-width: var(--container-xl, 1280px);
    margin: 0 auto;
  }

  .services-header {
    text-align: center;
    margin-bottom: var(--space-12, 3rem);
  }

  .services-title {
    font-family: var(--font-display, 'Space Grotesk', sans-serif);
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 700;
    color: var(--color-text-primary, #ffffff);
    margin: 0 0 var(--space-4, 1rem);
  }

  .services-intro {
    font-family: var(--font-body, 'Inter', sans-serif);
    font-size: var(--font-size-lg, 1.125rem);
    color: var(--color-text-secondary, #a0a0a0);
    margin: 0;
    max-width: 650px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
  }

  /* Card styles moved to React component, but grid layout remains */
  .services-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6, 1.5rem);
  }

  @media (min-width: 640px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
